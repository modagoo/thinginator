%fieldset{class: "property"}

  .field
    = f.label :value
    = f.text_field :value

    = f.hidden_field :sort, { class: 'sort' }

    = link_to_remove_fields "<i class=\"icon-remove\"></i> remove".html_safe, f, 'btn btn-small btn-danger prime-action'
